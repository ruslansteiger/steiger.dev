{
	"scripts": {
		"dev": "npm run serve",
		"serve": "cross-env NODE_ENV=development npx tailwindcss build -o public/app.css && live-server public/",
		"build": "cross-env NODE_ENV=production npx tailwindcss build -o public/app.css"
	},
	"devDependencies": {
		"cross-env": "^7.0.2",
		"live-server": "^1.2.1",
		"prettier": "^2.1.2",
		"tailwindcss": "^1.9.6"
	},
	"prettier": {
		"useTabs": true,
		"semi": false,
		"singleQuote": true,
		"quoteProps": "consistent"
	}
}
